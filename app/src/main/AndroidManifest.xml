<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.SafeView">
        <activity
            android:name=".screens.main.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="me.mitul.aij.screens.auth.AuthActivity" />

        <activity android:name=".screens.HomeActivity" />

        <activity android:name=".screens.DeveloperActivity" />

        <activity android:name=".screens.BranchListActivity" />

        <activity android:name=".screens.college.CollegeListActivity" />
        <activity android:name=".screens.college.CollegeDetailActivity" />

        <activity android:name=".screens.university.UniversityListActivity" />
        <activity android:name=".screens.university.UniversityDetailActivity" />

        <activity android:name=".screens.city.CityListActivity" />
        <activity android:name=".screens.city.AddressListActivity" />
    </application>
</manifest>
